fastapi==0.115.0
uvicorn[standard]==0.31.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
pdfplumber==0.11.4
python-docx==1.1.2

# NLP
spacy>=3.8,<3.9
scikit-learn==1.5.2

python-dotenv==1.0.1

# After installation:
# python -m spacy download en_core_web_sm

# NOTE: sentence-transformers/torch were removed from this project.
# Semantic matching now uses TF-IDF (scikit-learn) instead of transformer
# embeddings — a deliberate memory tradeoff for free-tier hosting (Render's
# 512MB limit). See the docstring at the top of app/nlp/matcher.py for the
# full reasoning and what a torch-based upgrade path would look like later.